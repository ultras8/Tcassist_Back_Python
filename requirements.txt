# --- Web Scraping & Automation ---
playwright==1.49.0      # หัวใจหลักที่ใช้คุมเบราว์เซอร์กวาด 2,146 ลิ้งก์
beautifulsoup4==4.12.3  # ใช้คู่กับ Playwright เพื่อแกะ HTML ให้แม่นยำขึ้น
requests==2.32.3        # สำหรับยิง API หรือเช็คสถานะลิ้งก์แบบด่วน

# --- Database & Environment ---
psycopg2-binary==2.9.9  # ตัวเชื่อมต่อ Python เข้ากับ PostgreSQL (NestJS Database)
python-dotenv==1.0.1    # เก็บพวก DB_PASSWORD และ GEMINI_API_KEY ไว้ใน .env ให้ปลอดภัย

# --- AI & Intelligence ---
google-generativeai     # ใช้ Gemini AI ช่วยวิเคราะห์เกณฑ์คะแนนที่ซับซ้อนจาก PDF